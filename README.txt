To compile the libraries execute the 'make' command in the following sub directories:
	HElib/src
	HElib-api
	FV-NFLlib-api
Then execute the 'make' command in the current directory. In case of errors, inspect the makefiles to try various compiler options.
	
